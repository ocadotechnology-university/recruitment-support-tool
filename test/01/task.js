general = (function() {

  return {

    /**
    * Returns sum of a and b.
    */
    add : function(a, b) {
      return a + b
    },

    /**
    * Returns difference of a and b.
    */
    subtract : function(a, b) {
      return a - b
    },

  };

})();

module.export = general;